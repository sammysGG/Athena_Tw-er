---
# Athena range — Tw@er target.
# Role tasks file: drop in as roles/<role>/tasks/main.yml (flat task list, no
# play wrapper). Same style as the port-range role.
#
# The lockdown lives in git: the twatter-update.service installed below runs
# `git reset --hard origin/main` + rebuild on every boot, so the app/scenario
# is owned by the GitHub repo and student edits on the box are wiped. Hardened
# credentials live in the persistent ./data volume and survive the rebuild.

- name: Check if docker is already installed
  ansible.builtin.command: docker --version
  register: docker_check
  changed_when: false
  failed_when: false

- name: Install docker prerequisites
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
    state: present
    update_cache: yes
  when: docker_check.rc != 0

- name: Create apt keyrings dir
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
  when: docker_check.rc != 0

- name: Add docker GPG key
  ansible.builtin.shell:
    cmd: |
      curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
      chmod a+r /etc/apt/keyrings/docker.gpg
    creates: /etc/apt/keyrings/docker.gpg
  when: docker_check.rc != 0

- name: Add docker apt repo
  ansible.builtin.shell:
    cmd: |
      echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" > /etc/apt/sources.list.d/docker.list
    creates: /etc/apt/sources.list.d/docker.list
  when: docker_check.rc != 0

- name: Install docker engine + compose plugin
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present
    update_cache: yes
  when: docker_check.rc != 0

- name: Enable and start docker
  ansible.builtin.systemd:
    name: docker
    enabled: yes
    state: started

- name: Add user to docker group
  ansible.builtin.user:
    name: user
    groups: docker
    append: yes

- name: Install git
  ansible.builtin.apt:
    name: git
    state: present
    update_cache: yes

- name: Clone twatter repo
  ansible.builtin.git:
    repo: https://github.com/sammysGG/Athena_Tw-er.git
    dest: /opt/twatter
    version: main
    force: yes

- name: Fix repo ownership
  ansible.builtin.file:
    path: /opt/twatter
    state: directory
    owner: root
    group: root
    recurse: yes

- name: Create data/upload dirs owned by the container uid (1001)
  ansible.builtin.file:
    path: "/opt/twatter/{{ item }}"
    state: directory
    owner: '1001'
    group: '1001'
    mode: '0755'
  loop:
    - data
    - uploads

- name: Check if .env already exists
  ansible.builtin.stat:
    path: /opt/twatter/.env
  register: env_file

- name: Generate .env with a random session secret
  ansible.builtin.copy:
    dest: /opt/twatter/.env
    owner: root
    group: root
    mode: '0600'
    content: |
      NEXTAUTH_SECRET="{{ lookup('pipe', 'openssl rand -hex 32') }}"
      NEXTAUTH_URL="http://{{ ansible_host | default(inventory_hostname) }}:18091"
      APP_BIND="0.0.0.0"
      APP_PORT="18091"
  when: not env_file.stat.exists

- name: Bind to all interfaces on the range VLAN
  ansible.builtin.lineinfile:
    path: /opt/twatter/.env
    regexp: '^APP_BIND='
    line: 'APP_BIND="0.0.0.0"'

- name: Pin the published port
  ansible.builtin.lineinfile:
    path: /opt/twatter/.env
    regexp: '^APP_PORT='
    line: 'APP_PORT="18091"'

- name: Pin the public URL
  ansible.builtin.lineinfile:
    path: /opt/twatter/.env
    regexp: '^NEXTAUTH_URL='
    line: 'NEXTAUTH_URL="http://{{ ansible_host | default(inventory_hostname) }}:18091"'

- name: Ensure .env perms are 600
  ansible.builtin.file:
    path: /opt/twatter/.env
    owner: root
    group: root
    mode: '0600'

- name: Allow the app port through ufw
  ansible.builtin.command: ufw allow 18091/tcp
  register: ufw_result
  changed_when: "'added' in ufw_result.stdout or 'updated' in ufw_result.stdout"
  failed_when: false

- name: Build and start the container
  ansible.builtin.command:
    cmd: docker compose up -d --build --force-recreate
    chdir: /opt/twatter

- name: Verify the app is serving its public API
  ansible.builtin.uri:
    url: http://127.0.0.1:18091/api/posts
    status_code: 200
  register: health
  retries: 20
  delay: 3
  until: health.status == 200

- name: Check current user count
  ansible.builtin.command:
    cmd: >-
      docker compose exec -T twatter
      node -e "const{PrismaClient}=require('@prisma/client');(async()=>{const p=new PrismaClient();process.stdout.write(String(await p.user.count()))})().catch(()=>process.stdout.write('0'))"
    chdir: /opt/twatter
  register: user_count
  changed_when: false
  failed_when: false

- name: Seed scenario users + content if the DB is empty
  ansible.builtin.command:
    cmd: docker compose exec -T twatter npx tsx prisma/seed.ts
    chdir: /opt/twatter
  when: (user_count.stdout | default('0') | trim) in ['', '0']

- name: Install boot-time update script
  ansible.builtin.copy:
    dest: /usr/local/bin/twatter-update.sh
    mode: '0755'
    owner: root
    group: root
    content: |
      #!/usr/bin/env bash
      set -euo pipefail
      cd /opt/twatter
      git fetch --quiet origin main
      git reset --hard origin/main
      docker compose up -d --build

- name: Install the update systemd unit
  ansible.builtin.copy:
    dest: /etc/systemd/system/twatter-update.service
    mode: '0644'
    owner: root
    group: root
    content: |
      [Unit]
      Description=Pull twatter updates from git and (re)start containers
      After=network-online.target docker.service
      Wants=network-online.target
      Requires=docker.service

      [Service]
      Type=oneshot
      WorkingDirectory=/opt/twatter
      ExecStart=/usr/local/bin/twatter-update.sh
      StandardOutput=journal
      StandardError=journal

      [Install]
      WantedBy=multi-user.target

- name: Enable the update unit on boot
  ansible.builtin.systemd:
    name: twatter-update.service
    enabled: yes
    daemon_reload: yes
